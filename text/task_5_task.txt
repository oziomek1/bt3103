Instructions:

* Create a subclass of playGame named detailsOfGame and detail its methods like so:

* Have def show_menu return the string “Showing menu”

* Have def start return the string “Starting game!”

* HINT: Remember, to inherit a parent class, you must include its name in the subclass parameters